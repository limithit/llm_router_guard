> 🌐 [English](prd.en.md) | **中文**

# AI 网关与模型护栏系统

## 产品需求文档（PRD）


## 文档版本

| 版本   | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| v1.0 | 2026-09-02 | - | 初始版本 |
| v1.１ | 2026-09-02 | - | 数据库新增 MySQL 可选；完善配置热加载机制 |


## 目录

1. [项目概述](#一项目概述)
2. [用户角色与使用场景](#二用户角色与使用场景)
3. [功能需求](#三功能需求)
4. [UI 界面规划](#四ui界面规划)
5. [非功能需求](#五非功能需求)
6. [技术实现方案](#六技术实现方案)
7. [部署方案](#七部署方案)
8. [风险与应对](#八风险与应对)
9. [成功标准](#九成功标准)
10. [附录](#十附录)


## 一、项目概述

### 1.1 项目背景

随着企业内部大模型应用从概念验证走向生产落地，以下问题日益突出：

| 问题领域 | 具体表现 |
|----------|----------|
| **安全风险** | 提示词注入、越狱攻击、敏感数据泄露、有害内容生成等安全威胁缺乏有效的拦截手段 |
| **管理碎片化** | 多模型、多账号、多业务线分散接入，缺乏统一的治理入口 |
| **成本失控** | 免费 Token 账号被限流后缺乏自动切换机制，导致服务不可用或成本激增 |
| **协议碎片化** | OpenAI、Anthropic 等不同厂商使用不同 API 协议，业务方需为每个厂商维护独立集成代码 |
| **审计缺失** | 缺少全链路的请求/响应日志，无法追溯安全事件和分析用量 |
| **配额混乱** | 缺乏统一的配额管理和速率限制机制，导致资源滥用和成本超支 |
| **配置分散** | 配置散落在多个配置文件中，变更需重启服务，无法实时生效，缺乏操作审计 |
| **管理安全** | 管理后台缺乏多因素认证，存在账号被盗导致配置被恶意篡改的风险 |
| **数据库锁定** | 仅支持 SQLite，无法满足高并发场景和 DBA 统一运维要求 |

### 1.2 项目目标

构建一个**轻量级、自托管、功能聚焦、配置全 UI 化、具备 MFA 安全能力、支持多种数据库**的 AI 网关系统，核心能力包括：

| 序号 | 能力 | 说明 |
|------|------|------|
| 1 | **多协议统一接入** | 同时支持 OpenAI Chat Completions / Responses API / Anthropic Messages API 三大协议 |
| 2 | **智能路由与 SLB** | 支持模型别名映射、加权负载均衡、自动故障转移 |
| 3 | **模型护栏** | 输入/输出双向过滤，支持关键词、正则、PII、注入检测，可扩展语义审核 |
| 4 | **配额与速率限制** | 支持按用户/模型配置请求次数和 Token 配额，定时自动重置 |
| 5 | **审计日志** | 全链路请求/响应记录，支持多维度查询分析；操作审计记录所有配置变更 |
| 6 | **全 UI 配置管理** | 所有配置通过 Web 界面完成，无需编辑配置文件，变更实时生效 |
| 7 | **配置热加载** | 模型接入、护栏字典、路由规则等配置变更后自动生效，无需重启服务 |
| 8 | **管理后台 MFA** | 支持 TOTP 多因素认证，提升管理后台安全性 |
| 9 | **多数据库支持** | 同时支持 SQLite / PostgreSQL / MySQL，满足不同规模部署需求 |

### 1.3 项目范围

**包含**：
- AI 网关核心服务（多协议接入、路由、SLB、护栏、审计、配额）
- 全功能 Web 管理界面（所有配置通过 UI 完成）
- 操作审计日志（记录所有配置变更）
- 管理后台 MFA（TOTP 多因素认证）
- 配置热加载机制（配置变更后自动生效，无需重启）
- 多数据库支持（SQLite / PostgreSQL / MySQL）
- RESTful 管理 API（供 UI 调用及外部集成）

**与 NewAPI 的定位差异**：

本系统面向**企业内网部署**，刻意保持"简约"——不做面向 C 端用户的多租户运营，也不做计费商业化，只做企业级的统一 AI 网关与安全护栏：

- **不做用户/租户管理体系**：NewAPI 面向多租户运营，需管理终端用户注册、分组、配额分发；本系统按"一企业一实例"模式，以 API Key 鉴权 + 管理员后台为准，后续如需企业 SSO 可对接，但不会长出租户注册/充值/分组树那套运营面。企业用它做内部统一出口，省去运营面带来的复杂度与攻击面。
- **不做计费/充值/兑换码体系**：NewAPI 内置令牌充值、兑换码、按量计费；本系统只做配额与速率限制（运维管控用，非计费），不涉及任何资金流与账单，部署维护成本更低。
- **不做在线支付集成**：不接入任何第三方支付网关，代码库不含支付凭证、回调验签等攻击面。
- **WebAuthn/Passkey 暂不支持**：管理后台以 TOTP MFA + 强制口令策略兜底（企业内部足够），Passkey 留待后续迭代。


## 二、用户角色与使用场景

### 2.1 用户角色

| 角色 | 职责 | 典型操作 |
|------|------|----------|
| **系统管理员** | 网关全局配置、用户管理、系统运维、安全策略制定 | 配置系统参数、管理 API Key、配置 MFA 策略、查看操作审计日志 |
| **AI 平台运维** | 模型管理、路由配置、配额管理 | 在 UI 中配置模型供应商、创建模型别名、设置 SLB 权重、配置配额 |
| **安全管理员** | 护栏策略制定与管理 | 在 UI 中维护敏感词库、配置 PII 检测规则、管理注入规则 |
| **业务开发者** | 通过网关接入 AI 能力 | 获取 API Key、在线查看使用文档、查看个人用量统计 |

### 2.2 使用场景

| 场景ID | 场景名称 | 描述 |
|--------|----------|------|
| S-001 | 新模型供应商接入 | 运维人员在 UI 中点击"添加供应商"，填入信息保存后**立即生效**，无需重启网关 |
| S-002 | 紧急敏感词添加 | 安全管理员发现新的敏感词后，在 UI 中即时添加，**3 秒内**所有请求生效拦截 |
| S-003 | 动态权重调整 | 运维人员在线调整供应商权重，**流量分配实时变化**，无需重启 |
| S-004 | 限流自动切换 | 运维在 UI 中配置故障转移策略，上游限流时自动切换 |
| S-005 | 配额在线调整 | 管理员在 UI 中调整用户配额，**立即生效**，无需重启 |
| S-006 | 审计日志查询 | 安全管理员在 UI 中筛选拦截记录，导出报表用于合规审查 |
| S-007 | 配置变更追溯 | 系统管理员在操作审计日志中查看谁在什么时间修改了敏感词配置 |
| S-008 | MFA 设备绑定 | 用户登录后进入个人账户安全设置，扫描二维码绑定 TOTP 身份验证器 |
| S-009 | 数据库迁移 | 运维人员从 SQLite 迁移到 MySQL/PostgreSQL，仅需修改连接配置，业务无感知 |


## 三、功能需求

### 3.1 系统设置模块

| 需求ID | REQ-001 |
|--------|---------|
| **名称** | 系统参数配置 |
| **描述** | 通过 UI 配置网关全局运行参数 |
| **优先级** | P0 |
| **UI 页面** | 系统设置 > 通用设置 |
| **配置项** | ① 网关监听端口 ② 日志级别（debug/info/warn/error）③ 审计日志保留天数（默认 90 天）④ 默认超时时间（秒）⑤ 最大并发连接数 ⑥ 是否启用护栏（全局开关）⑦ 热加载生效时间（秒，默认 3 秒） |

| 需求ID | REQ-002 |
|--------|---------|
| **名称** | API Key 管理 |
| **描述** | 创建、管理用于调用网关的 API Key，支持为不同业务方分配独立的 Key |
| **优先级** | P0 |
| **UI 页面** | 系统设置 > API Key 管理 |
| **功能点** | ① 创建 API Key（自动生成，可备注用途）② 启用/禁用 Key ③ 删除 Key ④ 查看 Key 列表（Key 值脱敏显示）⑤ 记录每个 Key 的最后使用时间 |

| 需求ID | REQ-003 |
|--------|---------|
| **名称** | 操作审计日志 |
| **描述** | 记录所有配置变更操作，支持查询和导出，满足合规追溯要求 |
| **优先级** | P0 |
| **UI 页面** | 系统设置 > 操作审计 |
| **记录内容** | 操作人、操作时间、操作类型（创建/修改/删除）、操作模块（供应商/模型/护栏/配额等）、变更前后对比、操作 IP、配置生效状态 |
| **功能点** | ① 按时间范围筛选 ② 按操作人筛选 ③ 按模块筛选 ④ 导出为 CSV |


### 3.2 配置热加载机制（核心能力）

| 需求ID | REQ-004 |
|--------|---------|
| **名称** | 配置热加载 |
| **描述** | 所有配置变更（模型供应商、模型别名、护栏字典、配额规则、速率限制等）在 UI 操作后自动生效，无需重启网关进程，对在线业务无影响 |
| **优先级** | P0 |
| **生效时间** | 配置变更后 ≤ 3 秒内所有新请求使用新配置；已在处理中的请求不受影响 |

**热加载生效范围**：

| 配置类型 | 变更内容 | 是否热加载 | 说明 |
|----------|----------|------------|------|
| **供应商配置** | 新增/编辑/删除供应商、修改 API Key、修改端点 | ✅ 是 | 立即影响该供应商的请求路由 |
| **模型别名** | 新增/编辑/删除别名、修改权重、增删上游 | ✅ 是 | 新请求立即使用新路由配置 |
| **SLB 权重** | 调整权重值 | ✅ 是 | 后续请求按新权重分发 |
| **护栏敏感词** | 新增/编辑/删除敏感词 | ✅ 是 | 新请求立即使用新词库；已有流式请求不受影响 |
| **PII 规则** | 新增/编辑/删除 PII 规则 | ✅ 是 | 新请求立即使用新规则 |
| **注入规则** | 新增/编辑/删除注入规则 | ✅ 是 | 新请求立即使用新规则 |
| **输出过滤** | 修改安全提示模板、响应策略 | ✅ 是 | 新请求立即使用新策略 |
| **配额规则** | 新增/编辑/删除配额、调整上限 | ✅ 是 | 新请求立即使用新配额 |
| **速率限制** | 新增/编辑/删除限流规则 | ✅ 是 | 新请求立即使用新限流策略 |
| **系统参数** | 修改日志级别、超时时间等 | ✅ 是 | 按参数类型，部分立即生效，部分下次启动生效 |

| 需求ID | REQ-004A |
|--------|----------|
| **名称** | 热加载状态可视化 |
| **描述** | 在 UI 中展示配置当前加载状态，若加载失败则提供详细错误信息和回滚提示 |
| **优先级** | P1 |
| **UI 页面** | 系统设置 > 配置状态 |
| **展示内容** | ① 当前配置版本号 ② 最后加载时间 ③ 配置加载状态（正常/异常）④ 若异常，展示失败原因和影响的模块 ⑤ 支持一键回滚到上一版本配置 |


### 3.3 模型供应商管理模块

| 需求ID | REQ-005 |
|--------|---------|
| **名称** | 供应商管理 |
| **描述** | 管理上游 AI 模型供应商的接入信息，包括端点、认证密钥、协议类型等 |
| **优先级** | P0 |
| **UI 页面** | 模型管理 > 供应商管理 |
| **管理字段** | ① 供应商名称（唯一标识）② 协议类型（OpenAI Chat / OpenAI Responses / Anthropic / 自定义）③ API 端点 URL ④ API Key（加密存储，输入时脱敏）⑤ 是否启用 ⑥ 备注 |
| **功能点** | ① 创建供应商 ② 编辑供应商 ③ 启用/禁用 ④ 删除（检查是否被模型别名引用）⑤ 测试连接 ⑥ 变更后热加载生效 |

### 3.4 模型路由与 SLB 模块

| 需求ID | REQ-006 |
|--------|---------|
| **名称** | 模型别名管理 |
| **描述** | 创建业务侧的模型别名，将一个别名映射到多个上游供应商，实现统一入口和负载均衡 |
| **优先级** | P0 |
| **UI 页面** | 模型管理 > 模型别名 |
| **管理字段** | ① 别名名称（如"gpt-4"）② 关联供应商列表 ③ 每个供应商的权重（整数，支持动态调整）④ 是否启用 ⑤ 备注 |
| **功能点** | ① 创建别名 ② 编辑别名 ③ 启用/禁用 ④ 删除 ⑤ 查看别名调用统计 ⑥ 变更后热加载生效 |

| 需求ID | REQ-007 |
|--------|---------|
| **名称** | 故障转移策略配置 |
| **描述** | 配置当一个上游供应商失败时的自动切换行为 |
| **优先级** | P0 |
| **UI 页面** | 模型管理 > 故障转移设置 |
| **配置项** | ① 是否启用自动故障转移 ② 重试次数 ③ 重试间隔策略（固定/指数退避）④ 触发条件 ⑤ 熔断阈值 ⑥ 熔断恢复时间 |


### 3.5 护栏管理模块

#### 3.5.1 敏感词管理

| 需求ID | REQ-008 |
|--------|---------|
| **名称** | 敏感词管理 |
| **描述** | 通过 UI 管理敏感词库，支持分类、批量操作、导入导出 |
| **优先级** | P0 |
| **UI 页面** | 护栏管理 > 敏感词 |
| **管理字段** | ① 关键词文本 ② 所属类别（涉政/色情/暴力/违法/歧视/自定义）③ 匹配模式（精确/包含/正则）④ 处理动作（拦截/告警/仅记录）⑤ 启用状态 ⑥ 创建时间 |
| **功能点** | ① 单个添加 ② 批量导入 ③ 导出 ④ 分类筛选 ⑤ 搜索 ⑥ 编辑 ⑦ 启用/禁用 ⑧ 删除 ⑨ 测试工具 ⑩ 变更后热加载生效 |

#### 3.5.2 PII 规则管理

| 需求ID | REQ-009 |
|--------|---------|
| **名称** | PII 识别规则管理 |
| **描述** | 配置个人身份信息的识别规则和处理策略 |
| **优先级** | P0 |
| **UI 页面** | 护栏管理 > PII 规则 |
| **功能点** | ① 创建/编辑/删除规则 ② 启用/禁用 ③ 预置常用规则模板 ④ 测试工具 ⑤ 变更后热加载生效 |

#### 3.5.3 注入攻击规则管理

| 需求ID | REQ-010 |
|--------|---------|
| **名称** | 注入攻击规则管理 |
| **描述** | 配置提示词注入、越狱攻击的检测规则 |
| **优先级** | P0 |
| **UI 页面** | 护栏管理 > 注入规则 |
| **功能点** | ① 创建/编辑/删除规则 ② 启用/禁用 ③ 预置常用规则模板 ④ 变更后热加载生效 |

#### 3.5.4 输出过滤管理

| 需求ID | REQ-011 |
|--------|---------|
| **名称** | 输出过滤配置 |
| **描述** | 配置模型输出内容的过滤策略 |
| **优先级** | P0 |
| **UI 页面** | 护栏管理 > 输出过滤 |
| **配置项** | ① 是否启用输出检测 ② 违规响应策略 ③ 自定义安全提示语模板 ④ 流式检测阈值 |


### 3.6 配额与速率限制模块

#### 3.6.1 配额管理

| 需求ID | REQ-012 |
|--------|---------|
| **名称** | 配额管理 |
| **描述** | 按用户和模型配置调用配额，支持定时自动重置 |
| **优先级** | P0 |
| **UI 页面** | 配额管理 > 配额列表 |
| **管理字段** | ① 用户标识 ② 模型别名 ③ 配额类型 ④ 周期类型 ⑤ 配额上限 ⑥ 超额策略 ⑦ 降级目标 ⑧ 启用状态 |
| **功能点** | ① 创建/编辑/删除配额 ② 启用/禁用 ③ 手动重置 ④ 查看实时使用量 ⑤ 批量创建 ⑥ 变更后热加载生效 |

#### 3.6.2 速率限制管理

| 需求ID | REQ-013 |
|--------|---------|
| **名称** | 速率限制管理 |
| **描述** | 配置短周期的速率限制，防止流量突增 |
| **优先级** | P0 |
| **UI 页面** | 配额管理 > 速率限制 |
| **功能点** | ① 创建/编辑/删除限流规则 ② 启用/禁用 ③ 查看限流命中统计 ④ 变更后热加载生效 |

#### 3.6.3 配额预警

| 需求ID | REQ-014 |
|--------|---------|
| **名称** | 配额预警配置 |
| **描述** | 配置配额使用率达到阈值时的告警通知 |
| **优先级** | P1 |
| **UI 页面** | 配额管理 > 预警设置 |
| **配置项** | ① 预警阈值（如 80%、95%）② 通知方式（页面内通知/邮件/Webhook）③ 通知接收人 |


### 3.7 审计日志模块

| 需求ID | REQ-015 |
|--------|---------|
| **名称** | 调用审计日志查询 |
| **描述** | 查询所有 API 调用记录，支持多维度筛选和导出 |
| **优先级** | P0 |
| **UI 页面** | 审计日志 > 调用日志 |
| **展示字段** | 请求 ID、时间、用户、模型别名、实际上游、输入（脱敏）、输出（脱敏）、Token 用量、状态、延迟、是否拦截、拦截原因 |
| **功能点** | ① 按时间范围筛选 ② 按用户筛选 ③ 按模型筛选 ④ 按拦截状态筛选 ⑤ 按拦截类别筛选 ⑥ 查看详情 ⑦ 导出 |

### 3.8 Dashboard 与统计模块

| 需求ID | REQ-016 |
|--------|---------|
| **名称** | 概览 Dashboard |
| **描述** | 展示网关运行的整体状态和关键指标 |
| **优先级** | P1 |
| **UI 页面** | 首页/Dashboard |
| **展示内容** | ① 今日请求总数、成功数、拦截数 ② 近 7 天调用趋势图 ③ 模型调用量排行 ④ 拦截类别分布饼图 ⑤ 上游健康状态总览 ⑥ 配额使用率总览 ⑦ 系统运行状态 |


### 3.9 多协议支持

| 需求ID | REQ-017 |
|--------|---------|
| **名称** | 多协议兼容接入 |
| **描述** | 网关同时支持三种核心协议：OpenAI Chat Completions API、OpenAI Responses API、Anthropic Messages API |
| **优先级** | P0 |
| **验收标准** | 标准 SDK 可直接调用对应端点，三种协议共用路由、护栏、SLB 和审计能力 |


### 3.10 系统运维模块

| 需求ID | REQ-018 |
|--------|---------|
| **名称** | 系统状态监控 |
| **描述** | 查看网关运行状态和性能指标 |
| **优先级** | P1 |
| **UI 页面** | 系统运维 > 状态监控 |
| **展示内容** | ① 服务运行状态 ② 内存/CPU 使用 ③ 当前连接数 ④ 各上游健康状态 ⑤ 各模型 QPS 实时数据 ⑥ 最近错误日志 ⑦ 配置加载状态 |

| 需求ID | REQ-019 |
|--------|---------|
| **名称** | 数据备份与恢复 |
| **描述** | 支持通过 UI 备份和恢复所有配置数据 |
| **优先级** | P2 |
| **UI 页面** | 系统运维 > 数据备份 |
| **功能点** | ① 手动备份（包含所有配置表数据）② 从备份文件恢复 ③ 查看备份历史 |


### 3.11 系统安全模块（MFA）

#### 3.11.1 MFA 全局配置

| 需求ID | REQ-020 |
|--------|---------|
| **名称** | MFA 全局配置 |
| **描述** | 管理员可在系统设置中开启 MFA 功能，并可选择是否对所有用户强制执行 |
| **优先级** | P1 |
| **UI 页面** | 系统设置 > 安全设置 > MFA 配置 |
| **配置项** | ① 是否启用 MFA ② 是否强制所有用户启用 ③ 支持的验证方式（TOTP）④ 备用恢复码数量（默认 10 个）⑤ 强制启用宽限期（天数） |
| **热加载** | ✅ 配置变更后立即生效 |

#### 3.11.2 用户 MFA 自助绑定

| 需求ID | REQ-021 |
|--------|---------|
| **名称** | 用户 MFA 自助绑定 |
| **描述** | 用户可在个人账户设置中自助绑定 MFA 设备 |
| **优先级** | P1 |
| **UI 页面** | 个人账户 > 安全设置 > 绑定 MFA |
| **功能点** | ① 生成并展示二维码 ② 用户输入动态验证码完成验证 ③ 生成备用恢复码并提示保存 |

#### 3.11.3 MFA 登录验证流程

| 需求ID | REQ-022 |
|--------|---------|
| **名称** | MFA 登录验证流程 |
| **描述** | 已绑定 MFA 的用户需在密码验证后输入动态验证码 |
| **优先级** | P1 |
| **功能点** | ① 密码验证 → MFA 状态检查 → 跳转 MFA 验证页 ② 验证码错误重试，连续 5 次失败后锁定账户 ③ 支持备用恢复码登录 |

#### 3.11.4 MFA 设备管理

| 需求ID | REQ-023 |
|--------|---------|
| **名称** | MFA 设备管理 |
| **描述** | 管理员和用户分别管理 MFA 设备 |
| **优先级** | P1 |
| **功能点** | ① 管理员：查看所有用户 MFA 状态、强制解绑（记录审计）② 用户：查看已绑定设备、解绑、重新绑定 |

#### 3.11.5 MFA 操作审计

| 需求ID | REQ-024 |
|--------|---------|
| **名称** | MFA 操作审计 |
| **描述** | 所有 MFA 相关操作记录到操作审计日志 |
| **优先级** | P1 |
| **记录内容** | 操作人、操作时间、操作类型、操作 IP |


## 四、UI 界面规划

### 4.1 整体布局

```
+----------------------------------------------------------+
|  AI 网关管理平台  [Logo]                    [用户头像]    |
+--------+-------------------------------------------------+
| 导航    |  内容区域                                       |
|         |                                                 |
| ├ 首页   |  [面包屑]                                      |
| ├ 模型管理 |                                                 |
| │ ├ 供应商 |  [页面标题]                                   |
| │ └ 模型别名 |                                                 |
| ├ 护栏管理 |                                                 |
| │ ├ 敏感词 |                                                 |
| │ ├ PII规则 |                                                 |
| │ ├ 注入规则 |                                                 |
| │ └ 输出过滤 |                                                 |
| ├ 配额管理 |                                                 |
| │ ├ 配额列表 |                                                 |
| │ ├ 速率限制 |                                                 |
| │ └ 预警设置 |                                                 |
| ├ 审计日志 |                                                 |
| │ ├ 调用日志 |                                                 |
| │ └ 操作审计 |                                                 |
| └ 系统设置 |                                                 |
|   ├ 通用设置 |                                                 |
|   ├ API Key  |                                                 |
|   ├ 安全设置 |  ← MFA 配置                                    |
|   ├ 配置状态 |  ← 热加载状态查看                              |
|   ├ 状态监控 |                                                 |
|   └ 数据备份 |                                                 |
+--------+-------------------------------------------------+
```

### 4.2 页面功能清单

| 页面 | 路由 | 核心功能 |
|------|------|----------|
| 首页/Dashboard | `/` | 关键指标卡片、趋势图、排行、状态总览 |
| 供应商管理 | `/providers` | 列表展示、CRUD、测试连接、启用/禁用、热加载状态 |
| 模型别名管理 | `/models` | 列表展示、CRUD、权重配置、调用统计 |
| 故障转移设置 | `/models/failover` | 策略配置表单 |
| 敏感词管理 | `/guard/keywords` | 列表展示、CRUD、分类筛选、批量导入导出、测试工具 |
| PII 规则管理 | `/guard/pii` | 规则列表、CRUD、预置模板、测试工具 |
| 注入规则管理 | `/guard/injection` | 规则列表、CRUD、预置模板 |
| 输出过滤配置 | `/guard/output` | 配置表单 |
| 配额管理 | `/quota` | 配额列表、CRUD、进度展示、手动重置 |
| 速率限制 | `/quota/ratelimit` | 规则列表、CRUD |
| 预警设置 | `/quota/alert` | 配置表单 |
| 调用审计日志 | `/audit/calls` | 列表展示、多维筛选、详情查看、导出 |
| 操作审计日志 | `/audit/operations` | 列表展示、筛选、导出 |
| 通用设置 | `/settings/general` | 配置表单 |
| API Key 管理 | `/settings/apikeys` | 列表展示、创建、启用/禁用、删除 |
| 安全设置 | `/settings/security` | MFA 全局配置、MFA 设备管理 |
| 配置状态 | `/settings/config-status` | 配置版本、加载状态、加载日志、一键回滚 |
| 状态监控 | `/settings/status` | 实时状态卡片、监控数据 |
| 数据备份 | `/settings/backup` | 备份/恢复操作 |
| 个人账户 > 安全设置 | `/account/security` | MFA 绑定/解绑、恢复码管理 |

### 4.3 热加载状态页面示意

```
┌─────────────────────────────────────────────────────────────────────┐
│  系统设置 > 配置状态                                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  当前配置版本: v20260902.143022                                     │
│  最后加载时间: 2026-09-02 14:30:22                                 │
│  加载状态: ✅ 正常                                                  │
│  已生效配置模块: 供应商(12) 模型别名(8) 敏感词(156) PII规则(6)     │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  📋 最近加载日志                                            │   │
│  │  14:30:22  [成功] 供应商配置加载完成 (12项)                  │   │
│  │  14:30:22  [成功] 模型别名配置加载完成 (8项)                 │   │
│  │  14:30:22  [成功] 敏感词库加载完成 (156项, AC自动机构建)    │   │
│  │  14:25:10  [成功] 配额规则加载完成 (23项)                    │   │
│  │  14:20:05  [成功] 供应商 "deepseek-account-3" 新增          │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  [手动重新加载配置]  [导出当前配置]  [回滚到上一版本]              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```


## 五、非功能需求

| 需求ID | 类别 | 描述 | 目标值 |
|--------|------|------|--------|
| NFR-001 | 性能 | 护栏基础版端到端延迟增加 | ≤ 100ms |
| NFR-002 | 性能 | 护栏增强版（含语义审核）延迟增加 | ≤ 300ms |
| NFR-003 | 性能 | 单实例 QPS | ≥ 1000 |
| NFR-004 | 可用性 | 网关可用性 | ≥ 99.9% |
| NFR-005 | 可用性 | 护栏服务故障时自动降级 | 不阻断业务 |
| NFR-006 | 准确性 | 敏感词命中率 | ≥ 95% |
| NFR-007 | 准确性 | 敏感词误报率 | ≤ 1% |
| NFR-008 | 安全性 | API Key 传输 | 仅通过 HTTPS |
| NFR-009 | 安全性 | 审计日志 PII | 自动脱敏存储 |
| NFR-010 | 安全性 | 管理后台 MFA | 支持 TOTP |
| NFR-011 | 可扩展性 | 新增协议适配 | 仅需实现解析器和序列化器 |
| NFR-012 | 易用性 | 配置生效方式 | UI 操作即生效，热加载 ≤ 3 秒 |
| NFR-013 | 可观测性 | Prometheus 指标 | 全覆盖 |
| NFR-014 | UI 响应 | 管理界面操作响应 | ≤ 1 秒 |
| NFR-015 | 并发 | UI 管理操作并发 | 支持 10 人同时操作 |
| NFR-016 | 数据库 | 支持的数据库类型 | SQLite / PostgreSQL / MySQL |


## 六、技术实现方案

### 6.1 整体架构

网关采用**前后端分离**架构，后端为单进程 Go 服务（内置前端静态资源），数据存储支持 SQLite / PostgreSQL / MySQL 三种数据库。

**架构分层**：

| 层次 | 职责 | 核心组件 |
|------|------|----------|
| **接入层** | 接收 HTTP 请求，支持三种协议端点 + 管理 API | Gin HTTP Server |
| **中间件链** | 顺序执行认证、护栏、路由、审计 | 认证中间件、护栏中间件、路由中间件、审计中间件 |
| **协议适配层** | 请求解析与响应序列化，协议自动识别 | 协议检测器、规范模型转换器 |
| **核心业务层** | SLB、护栏检测、配额管理 | 负载均衡器、护栏引擎、配额管理器 |
| **存储层** | 审计日志、配置数据、配额数据持久化 | SQLite / PostgreSQL / MySQL |
| **管理服务层** | 供 UI 调用的管理 API、配置热更新通知 | 管理 API Handler、配置变更通知机制 |

### 6.2 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| 编程语言 | Go 1.21+ | 单二进制、优秀并发模型 |
| Web 框架 | Gin | 轻量、性能好、中间件生态成熟 |
| 管理 API | RESTful | 供前端 UI 调用 |
| 前端框架 | **React 18+** | 组件化、生态成熟、TypeScript 支持完善 |
| 前端 UI 组件库 | **Ant Design 5.x** | 企业级 React UI 组件库，开箱即用 |
| 前端状态管理 | **Zustand** 或 **TanStack Query** | 轻量状态管理 + 服务端状态缓存 |
| 前端路由 | **React Router v6** | 声明式路由 |
| 前端构建 | **Vite** | 快速构建 |
| 前端部署 | 内嵌于 Go 二进制 | 通过 embed 将构建产物打包，单文件交付 |
| 配置存储 | 数据库（非配置文件） | 所有配置存于数据库，支持热加载 |
| ORM | GORM | 简化数据库操作，支持多种数据库 |
| 数据库驱动 | GORM 驱动 | SQLite / PostgreSQL / MySQL 统一接口 |
| TOTP 库 | pquerna/otp | MFA 验证码生成与验证 |
| 二维码生成 | skip2/go-qrcode | MFA 绑定二维码生成 |

### 6.3 核心模块设计

#### 6.3.1 多数据库支持

| 数据库 | 适用场景 | 连接配置示例 |
|--------|----------|-------------|
| **SQLite** | 开发测试、小规模部署、单机环境 | `sqlite:./gateway.db` |
| **PostgreSQL** | 生产环境、高并发场景 | `postgres://user:pass@localhost:5432/gateway` |
| **MySQL** | 生产环境、企业统一数据库栈 | `mysql://user:pass@tcp(localhost:3306)/gateway` |

**数据库切换方式**：
- 通过环境变量 `DB_TYPE` 和 `DB_DSN` 配置
- 首次启动时自动执行数据库迁移（建表、索引）
- 支持数据库版本迁移管理（GORM AutoMigrate + 手动迁移脚本）

#### 6.3.2 配置热加载机制

所有配置存储在数据库中，变更通过管理 API 触发实时生效：

```
┌─────────────────────────────────────────────────────────────────────┐
│                      配置热加载流程                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 用户在 UI 中提交配置变更（如新增敏感词）                        │
│     ↓                                                              │
│  2. 管理 API 将变更写入数据库                                      │
│     ↓                                                              │
│  3. 触发配置变更事件（通过数据库触发器 / 消息通知 / 定时轮询）     │
│     ↓                                                              │
│  4. 核心网关接收变更通知                                           │
│     ↓                                                              │
│  5. 从数据库重新加载对应模块配置                                    │
│     ↓                                                              │
│  6. 原子替换内存缓存（新配置在下次请求时生效）                     │
│     ↓                                                              │
│  7. 变更操作记录到操作审计日志                                     │
│     ↓                                                              │
│  8. 加载状态更新到配置状态表，供 UI 展示                           │
│                                                                     │
│  总耗时：≤ 3 秒                                                     │
│  已有请求：不受影响（使用旧配置完成处理）                          │
│  新请求：立即使用新配置                                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**热加载触发方式**：

| 方式 | 说明 | 适用场景 |
|------|------|----------|
| **实时触发** | 配置变更后主动推送通知（通过 channel 或消息队列） | 推荐，延迟最低 |
| **定时轮询** | 网关每隔 N 秒查询数据库配置版本号，发现变化则重新加载 | 备选方案，实现简单 |
| **手动触发** | 用户在 UI 中点击"重新加载配置"按钮 | 应急场景 |


## 七、部署方案

### 7.1 部署方式

| 方式 | 适用场景 | 说明 |
|------|----------|------|
| **单二进制** | 开发测试、小规模 | 直接运行，内置 SQLite |
| **Docker 容器** | 标准化交付 | 镜像内嵌管理 UI |
| **Kubernetes** | 生产环境 | 多副本、ConfigMap、Secret |

### 7.2 数据库选型建议

| 场景 | 推荐数据库 | 说明 |
|------|-----------|------|
| 开发/测试 | SQLite | 零配置，快速启动 |
| 日请求 < 10 万 | SQLite | 性能足够 |
| 日请求 > 10 万 | PostgreSQL / MySQL | 高并发读写支持 |
| 企业统一数据库栈 | MySQL / PostgreSQL | 符合 DBA 运维规范 |

### 7.3 首次启动引导

- 首次启动时自动初始化数据库
- 提示访问管理界面地址
- 引导用户创建管理员账号和默认 API Key


## 八、风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 上游模型协议频繁变化 | 适配器需持续维护 | 中 | 适配器模式隔离变化；优先支持主流协议 |
| 护栏误报过高 | 影响正常业务 | 中 | 支持三种动作灵活配置；UI 中提供测试工具 |
| 审计日志数据量增长 | 存储压力 | 中 | SQLite 适合日请求 < 10 万；提供自动清理策略 |
| 语义审核模型延迟高 | 影响用户体验 | 中 | 降级到基础规则；支持按请求粒度开关 |


## 九、成功标准

| 维度 | 指标 | 目标 |
|------|------|------|
| **功能完整性** | P0 需求交付率 | 100% |
| **配置效率** | 新增供应商到生效时间 | ≤ 3 分钟 |
| **性能** | 端到端延迟增加（基础版） | ≤ 100ms |
| **准确性** | 敏感词命中率 | ≥ 95% |
| **准确性** | 敏感词误报率 | ≤ 1% |
| **可用性** | 网关可用性 | ≥ 99.9% |
| **可维护性** | 配置变更生效时间 | ≤ 3 秒 |
| **用户满意度** | 运维操作效率提升 | ≥ 50% |


## 十、附录

### 10.1 术语表

| 术语 | 说明 |
|------|------|
| **护栏（Guardrails）** | 对模型输入输出进行安全检测和过滤的机制 |
| **SLB** | 服务负载均衡，将请求分发到多个上游 |
| **PII** | 个人身份信息，如手机号、身份证号 |
| **提示词注入** | 恶意用户通过构造提示词攻击模型 |
| **规范模型（Canonical Model）** | 网关内部统一的请求/响应数据格式 |
| **配额（Quota）** | 长期总量限制，按天/周/月重置 |
| **速率限制（Rate Limit）** | 短期速率限制，按秒/分钟滑动窗口 |
| **供应商（Provider）** | 上游 AI 模型服务商的一个接入账号 |
| **模型别名（Model Alias）** | 业务侧统一调用的模型名称，可映射到多个供应商 |
| **热加载（Hot Reload）** | 配置变更后无需重启服务即可生效 |
| **MFA（多因素认证）** | Multi-Factor Authentication，本系统采用 TOTP 方式 |

### 10.2 参考文档

| 文档 | 说明 |
|------|------|
| OpenAI Chat Completions API | https://platform.openai.com/docs/api-reference/chat |
| OpenAI Responses API | https://platform.openai.com/docs/api-reference/responses |
| Anthropic Messages API | https://docs.anthropic.com/en/api/messages |
| Qwen3Guard | 阿里云开源安全审核模型 |
| Ant Design React | https://ant.design/ |
| React Router | https://reactrouter.com/ |
| TanStack Query | https://tanstack.com/query |


**文档结束**
